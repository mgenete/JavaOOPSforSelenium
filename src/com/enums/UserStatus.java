package com.enums;

	//Consider the Enum type if your program consists of a fixed set of constants, like seasons of the year
public enum UserStatus {
	PENDING,
    ACTIVE,
    INACTIVE,
    DELETED;

}
